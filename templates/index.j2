<!DOCTYPE html>
<html lang="cs" class="flex w-[100%] h-[100%]">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title> Accounting v3</title>
        <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
        <link rel="stylesheet" href="{{ url_for('static', filename='css/output.css') }}">
        <link rel="icon" href="{{ url_for('static', filename='favicon.ico') }}" type="image/x-icon">
        <script src="{{ url_for('static', filename='js/index/set_collection.js') }}"></script>
    </head>
    <body class="flex flex-col w-[100%] h-[100%] bg-zinc-900 ">
        <div class="flex justify-center items-center w-[100%] bg-black text-white">
            <div class="flex">
                <a href="{{ url_for('home') }}" class="px-[20px] py-[10px] hover:bg-zinc-900">Home</a>
                <a href="{{ url_for('edit') }}" class="px-[20px] py-[10px] hover:bg-zinc-900">Data</a>
                <a href="{{ url_for('stats') }}" class="px-[20px] py-[10px] hover:bg-zinc-900">Stats</a>
                <a href="{{ url_for('settings') }}" class="px-[20px] py-[10px] hover:bg-zinc-900">Settings</a>
                <a href="#" class="px-[20px] py-[10px] hover:bg-zinc-900">About</a>
                <a href="#" class="px-[20px] py-[10px] hover:bg-zinc-900">Contact</a>
            </div>
        </div>
        <div class="flex justify-center w-[100%] h-[100%]">
            <div class="flex justify-center items-center w-[50%]">
                <div class="flex justify-center w-[100%]">
                    <a href="#" class="py-[40px] bg-black text-white text-center w-[50%]">Upload new data</a>
                </div>
            </div>
            <div class="flex flex-col justify-center items-center w-[50%]">
                {% for collection in collections %}
                <div class="py-[40px] bg-black text-white text-center w-[50%]" onclick="setCollection('{{ collection }}')">{{ collection }}</div>
                {% endfor %}
            </div>            
        </div>
    </body>
</html>